# Python 3.11 is recommended. These are version ranges, not an experiment lockfile.
# Install a suitable CUDA build of PyTorch first if GPU training is required:
# https://pytorch.org/get-started/locally/
torch>=2.2,<3.0

# Numerical computing, tabular data, and evaluation.
numpy>=1.26,<3.0
pandas>=2.2,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.4,<2.0

# Graph construction and metapath Skip-gram embeddings.
networkx>=3.2,<4.0
gensim>=4.4,<5.0

# Configuration and Morgan fingerprint generation.
PyYAML>=6.0,<7.0
rdkit>=2023.9.1

# pyMeSHSim is not imported by the training pipeline.
# Its precomputed Wang similarity cache must be supplied separately; see README.md.
